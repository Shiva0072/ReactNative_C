// import React from "react";
// import { View, Platform } from "react-native";
// import {
//   LongPressGestureHandler,
//   Swipeable,
// } from "react-native-gesture-handler";

// function ListItemGestures({ children, renderRightActions, longPressGesture }) {
//   return (
//     <View>
//       {Platform.OS === "ios" ? (
//         <Swipeable renderRightActions={renderRightActions}>
//           {children}
//         </Swipeable>
//       ) : (
//         <LongPressGestureHandler gesture={longPressGesture}>
//           {children}
//         </LongPressGestureHandler>
//       )}
//     </View>
//   );
// }

// export default ListItemGestures;
